<h1>Songs</h1>

<% @songs.each do |song| %>

  <h3>
    <a href="songs/<%= song.id %>">
      <%= song.title %>
    </a>

    <p>
      Song Length: <%= song.length %>
    </p>

    <p>
      Play Count: <%= song.play_count %>
    </p>
  </h3>

<% end %>
